{
  "name": "profile-studio",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:coverage": "c8 --reporter=text --reporter=text-summary --reporter=html node --test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "eslint": "^10.8.1",
    "globals": "^17.11.0"
  }
}
